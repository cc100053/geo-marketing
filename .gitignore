.DS_Store
.env
.env.*
!.env.example
node_modules/
vendor/
.firebase/
firebase-debug.log
*-debug.log

# Local CMS checkout includes .env, vendor, storage, and nested git state.
# Keep it local; document setup instead of tracking it from this workspace root.
shared/GEOFlow/
